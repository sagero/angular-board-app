import { CardDueDirective } from './card-due.directive';

describe('CardDueDirective', () => {
  it('should create an instance', () => {
    const directive = new CardDueDirective();
    expect(directive).toBeTruthy();
  });
});
